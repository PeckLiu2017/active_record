<h1>create address</h1>
<%= simple_form_for @address do |f|%>
<div class="form-group">
  <%= f.input :province %>
</div>
<!-- <div class="form-group">
  <%#= f.select(:client_id, (Client.all.map{ |c| [c.name, c.id] }))%>
</div> -->

<div class="form-group">
  <%= f.collection_select(:client_id, Client.all, :id, :name)%>
</div>
<div class="form-group">
  <%= f.submit 'submit' %>
</div>
<% end %>
