<p id="notice"><%= notice %></p>

<h1>Orders</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @orders.each do |order| %>
    <%# binding.pry %>
      <tr>
        <td><%= order.product %></td>
        <td><%#= link_to 'Show', client_order_path(order) %></td>
        <td><%= link_to 'Edit', edit_client_order_path(order) %></td>
        <td><%#= link_to 'Destroy', order, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Order', new_client_order_path(@client) %>
