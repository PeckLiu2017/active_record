<%= simple_form_for [@client, @order] do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :product %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
